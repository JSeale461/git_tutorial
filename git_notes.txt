git notes:

git add {filename}
git rm --cached index.html
 git commit -m "commit_editmsg"
 git commit -m 'changes 2'
  git branch login            // create a branch
 git checkout login           // move into branch